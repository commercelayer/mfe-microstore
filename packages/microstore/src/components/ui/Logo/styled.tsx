import Styled from "styled-components"
import tw from "twin.macro"

export const Image = Styled.img`
  ${tw`w-auto max-w-full`}
`
export const Label = Styled.h1`
  ${tw`font-extrabold uppercase tracking-wide text-xl text-black`}
`
